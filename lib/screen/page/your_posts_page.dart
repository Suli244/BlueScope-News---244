import 'package:flutter/material.dart';

class YourPostsPage extends StatelessWidget {
  const YourPostsPage({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('YourPostsPage'),
      ),
      body: Column(
        children: [
          Container(),
        ],
      ),
    );
  }
}